<a href="/songs/new">Add a song</a>
<% @songs.each do |song| %>
  <div class="song">
    <h2><a href="/songs/<%= song.id %>"><%= song.title %></a></h2> 
    <p><%= song.artist %></p>
  </div>
<% end %>