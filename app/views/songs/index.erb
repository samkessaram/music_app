<% @songs.each do |song| %>
  <div class="song col-xs-6 col-md-4">
    <h2><a href="/songs/<%= song.id %>"><%= song.title %></a></h2> 
    <p><%= song.artist %></p>
  </div>
<% end %>
<a href="/songs/new" class="btn btn-primary btn-lg">Add a song</a>

